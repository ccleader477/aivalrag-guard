"""Shared prompt-injection defense primitives for aivalrag-chat and
AIValRAG-Backend. See PROMPT_INJECTION_PLAN.md (D1) -- one implementation,
not two, so the two repos' defenses can't drift apart."""
